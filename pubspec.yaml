name: dota2checker
description: dota2checker

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  path_provider: ^2.0.5
  file_picker: ^4.1.4
  http: ^0.13.4
  get: ^4.3.8
  provider: ^6.0.0
  graphql_flutter: ^5.0.0
  libwinmedia: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
